# HOS Hypervisor monitor_agent 性能监控代理概要设计

| 项目 | 内容 |
|---|---|
| 文档版本 | v0.2（基于原始 v0.1 的轻量级修订版） |
| 适用范围 | HOS Hypervisor |
| 文档状态 | 概要设计评审稿，尚未实现或完成板端验证 |
| 设计基础 | 用户提供的 v0.1 文档及 `cpu_hcall.c/.h`、`vmm_u_ioreq.c/.h` |

**本文中的 HOS 仅指以 Xvisor 为底座适配的 Hypervisor。** 本设计沿用原稿的 CPU、内存、中断三类九项监控，以及周期线程、统计、告警、处置和上报的总体思路。DSM 进程监控、Guest 内部服务监控及 DSM 重拉起由 ServiceVM 负责，不属于 monitor_agent 的职责。

原稿列出的调度、内存、IRQ 等接口作为复用依据保留；未提供对应源码的部分标为“原稿列明，接入时核对”。本文提出的新增接口、默认参数和处理规则均为设计建议，不表示已有实现。

## 1. 需求概述

### 1.1 建设目标

HOS 内置一个可配置的轻量级性能监控代理 `monitor_agent`，运行期间采集 Hypervisor 能直接获得的性能统计和异常信息，周期判断阈值，执行适用的异常处理，并提供本地查询和向 ServiceVM 上报的能力。

基本处理过程为：

```text
已有统计 / 少量新增埋点 → 周期汇总 → 阈值判断 → 记录、上报及可选处置
```

监控不替代原有调度器、内存检查和中断处理逻辑；不为获取 Guest 内部指标而解析 Guest 进程或内核数据结构。现有接口不支持的功能明确保留，不以相近指标冒充完成。

### 1.2 监控范围与职责

| 编号 | 原稿监控项 | HOS 本次设计内容 | 边界或保留内容 |
|---|---|---|---|
| CPU-1 | vCPU 调度延迟、就绪队列等待时间 | 累计等待、窗口最大等待、单次等待 P99、当前 READY 等待年龄 | 精确单次分布需增加调度埋点 |
| CPU-2 | 一键采集、识别异常指令周期 | 汇总 CPU、调度、trap/exit 统计；标记高频陷入及已具备的耗时异常 | 任意 Guest 指令的执行周期无法由现有 trap 计数获得 |
| CPU-3 | Linux/Android 域 CPU 占用率与细粒度限流 | 汇总各 Guest 的 vCPU 运行时间，判断高占用，记录并上报 | Guest 进程级限流归 Guest；HOS 严格 CPU 配额机制保留 |
| 内存-1 | Guest 内存用量、CMA 水位 | 展示 Guest 分配内存；监控 HOS RAM 和 HOS DMA heap 水位 | Guest 实际内存使用量、Guest CMA 水位由 Guest 采集 |
| 内存-2 | 各域内存及共享内存边界异常 | 汇总已有检查路径确认的越界、非法映射和访问异常 | 映射范围内的 Guest 内部越界、绕过检查接口的访问不保证可见 |
| 内存-3 | 异常 IOMMU 页表请求超过 20% 时限流 | 请求计数、非法请求比例、映射失败及 DMA fault 统计 | 请求限流依赖可控入口和错误返回约定，暂列保留 |
| 中断-1 | 物理触发至虚拟注入延迟 P99/抖动 | 优先测量 HOS 接收中断至虚拟注入的内部延迟 | 真实物理触发时间、Guest 响应时间需对应时间戳或配合 |
| 中断-2 | exit 风暴，超过 5 万次/s 持续 5 s | 按 vCPU、exit 类型统计速率，连续超限后记录快照并上报 | 重置为受控可选动作，不因计数超限默认 panic |
| 中断-3 | 异常注入、丢失中断及超过 80% 的异常占比 | 注入错误、IRQ 来源占比、pending 超时、LR 资源不足 | 确认“真正丢失”需事件对应证据；屏蔽与重置须满足使用条件 |

本版没有增加 DSM/I/O 后端健康监控项。Linux 作为 ServiceVM 接收 HOS 告警，以及 Linux 自身被监控为一个 Guest，是两种不同角色。

### 1.3 基本要求

常驻功能采用一个周期线程，默认每 1 s 汇总一次。高频路径仅做必要的计数、时间戳或时延分桶，不逐条打印日志，不在中断、调度锁内执行上报、文件导出或重置。

默认启用统计和日志；Guest 上报在接口接通后启用。限流、屏蔽、重置等动作单独配置，未实现或不适用时明确返回“不支持”，不静默替换成其他动作。

## 2. 现状盘点

### 2.1 已有可复用能力

以下能力来自原稿 [1]；表中除特别说明外，均需在详细接入时核对当前 HOS 源码。

| 类别 | 原有能力或接口 | 本设计用途 | 核对重点 |
|---|---|---|---|
| 调度 | `vmm_scheduler_stats()`、`state_ready_nsecs`、`state_running_nsecs` 及 max 字段 | 周期读取状态、累计运行和等待时间 | 是否为实时累计；max 是历史还是窗口值；读取是否有较长跨核等待 |
| CPU | `vmm_scheduler_idle_time()`、`vmm_scheduler_irq_time()`、ready count | pCPU 利用率、IRQ 时间及队列情况 | 返回值是累计量还是已完成采样窗口值 |
| trap | `vmm_manager_vcpu_get_trap_cnt()` 等 | exit 速率和类型分布 | 返回是否需要额外快照操作；分类是否重复计数 |
| 内存 | `vmm_host_ram_total_used/free_frames()`、Guest region 信息 | HOS RAM 水位和 Guest 分配布局 | 保留池、共享区、重复映射的统计口径 |
| DMA heap | `vmm_dma_heap_free_size()` | HOS 连续内存池剩余量 | 池总容量的可用读取位置 |
| 边界 | `vmm_shmem_read/write/set()`、region 检查、Stage-2 异常处理 | 对已有拒绝或异常分支补充计数 | 正常 MMIO 模拟与非法访问分类 |
| IOMMU | `vmm_iommu_map/unmap()`、SMMU fault trace | 请求、失败及 fault 计数 | 选择单一请求层计数，避免上层和驱动重复累计 |
| IRQ | `cmd_irq_latency`、VGIC pending 信息、IRQ 计数 | 内部延迟、pending 年龄及注入情况 | 时间戳含义及能否配对到同一次事件 |
| IRQ 超时 | `vmm_manager_detect_virq_timeout()`，原稿提到 700 ms | 复用已有超时判断结果 | 是否包含正常屏蔽、暂停等情况，阈值实际生效方式 |
| 框架 | `vmm_threads`、`vmm_workqueue`、命令框架、devtree/openconf | 周期采集、耗时任务、诊断及配置 | 复用，不新增通用监控框架 |
| 日志与动作 | logsystem、Guest/vCPU 管理接口 | 保存异常信息，执行经允许的动作 | 导出上下文、动作影响及返回语义 |

### 2.2 附件源码能够确认的接口边界

| 位置 | 已确认内容 | 对本设计的影响 |
|---|---|---|
| `cpu_hcall.c::ccm_upcall()` | 通过 `servervm` 和 `ccm_irq` 注入通知 [2] | 可作为通知 ServiceVM 的候选入口，不等于具备向两个 Guest 通用上报的功能 |
| `vmm_u_ioreq.c::init_vm_event()`、`sbuf_setup_common()` | 有 `VMMU_VM_EVENT` 缓冲初始化和分派代码 [3][4] | 可评估复用缓冲类型；尚不能据此认定健康事件协议及 Guest 接收端已完成 |
| `cpu_hcall.c::hcall_vm_intr_monitor()` | 当前忽略参数并直接返回成功 [2] | 不能作为已经实现的 IRQ 监控输出接口 |
| `cpu_hcall.c::hcall_get_meminfo()` | 读取 Guest `mem0` 信息，并可能为 ServiceVM 添加映射 [2] | 不是 Guest 内部内存使用量接口，也不宜作为周期监控的纯查询接口 |
| `cpu_hcall.c::hcall_reset_vm()` | 可能创建后台线程执行 reset 后立即返回 [2] | 不能把调用成功直接记为实际恢复成功 |
| `cpu_hcall.c::HC_REBOOT_VM` 分支 | 指定条件下进入调用 `board_reset()` 的路径 [2] | 不作为监控器的普通单 Guest 重置入口 |

这些源码仅用于核对本方案相关的统计、通知和动作边界；本方案不扩展为 DSM 或 I/O 代码整改设计。

### 2.3 待实现内容

新增周期采集线程、统计快照、阈值判断、固定容量事件缓存和 `monitor` 命令。调度等待及 IRQ 延迟的 P99 需要有效的单次样本；边界、IOMMU 和部分 IRQ 错误需要少量计数埋点。

Guest 告警接收、严格 CPU 配额、IOMMU 请求限流及受控 reset 分别列出接入条件。前述能力尚未具备时，基础统计与本地告警仍可独立使用。

## 3. 总体架构设计

### 3.1 设计思路

```text
调度 / trap / 内存 / IOMMU / IRQ
          │ 已有计数 + 必要时间戳 / 错误计数
          ▼
   monitor_agent 周期线程
   ┌─────────────────────────────┐
   │ 读取快照 → 统计计算 → 阈值判定 │
   └─────────────────────────────┘
          │
          ├─ 更新查询结果 → monitor 命令
          ├─ 记录事件 / 异常快照
          ├─ 向 ServiceVM 上报（接口接通后）
          └─ 经配置允许的处置 → 既有管理接口
```

采用固定监控项，不引入插件注册、跨 Guest 健康状态机、依赖管理或分布式恢复协议。CPU、内存、IRQ 的统计对象以 HOS 的 Guest ID、vCPU ID、pCPU ID、IRQ ID 标识；名称仅用于显示。

### 3.2 模块划分

保留原稿的文件划分，小型实现可合并文件，不要求形成独立框架。

| 文件 | 职责 |
|---|---|
| `core/monitor/monitor_agent.c` | 初始化、周期线程、启停和采样调度 |
| `core/monitor/monitor_stats.c` | 增量、速率、均值、最大值、P99 分桶统计 |
| `core/monitor/monitor_cpu.c` | vCPU 等待、Guest CPU 占用率及 CPU 诊断数据 |
| `core/monitor/monitor_mem.c` | HOS 内存、Guest 分配量、边界和 IOMMU 计数 |
| `core/monitor/monitor_irq.c` | IRQ 延迟、exit 风暴、注入错误和 pending 超时 |
| `core/monitor/monitor_config.c` | 编译默认值、设备树及命令配置 |
| `core/monitor/monitor_action.c` | 告警去重、事件缓存、上报和适用的处置 |
| `commands/cmd_monitor.c` | 启停、查询、采集和配置命令 |

### 3.3 运行流程

初始化时读取配置、分配固定统计空间、建立累计计数基线并启动线程。每个采样周期按以下顺序处理：

```text
读取当前时间和已有统计
    → 计算实际采样间隔及各项增量
    → 汇总本周期时延分布和错误计数
    → 判断阈值与连续超限时间
    → 更新查询结果，记录/上报告警
    → 提交必要的耗时处置，进入下一周期
```

第一次读取只建立基线。计数清零、Guest reset 或对象更换后重新建立基线，不用负差值或无符号回绕值计算异常速率。对象读取失败时本周期显示“不可用”，不填成 0；正常暂停或系统休眠期间不累计相关超限时间，恢复后重新采样。

线程查询 Guest/vCPU 时使用管理层已有的对象引用和锁定方式，不长期保存可能失效的对象指针。耗时日志导出或 reset 交给已有工作队列/管理流程，避免阻塞后续采样。

### 3.4 统计方式

**累计量与速率。** 对累计计数使用相邻有效快照差值，速率统一为 `计数增量 / 实际间隔秒数`。不要假定线程每次恰好间隔 1 s，也不要为了监控清零其他命令共用的计数器。

**时延分布。** 在实际事件发生处得到单次时延，再放入固定分桶。周期线程读取桶计数，计算均值、最大值和 P99。建议默认使用约 16 个桶，桶边界围绕板端关注的微秒/毫秒范围设置；不保存无限增长的原始样本。

P99 的计算位置为 `ceil(0.99 × 样本数)`。累计桶计数首次达到该位置时，使用该桶上边界作为 P99 估计值，并标明“分桶估计”。最高桶为溢出桶时输出“超过最高边界”，不伪造精确值。建议至少 100 个有效样本才输出 P99；不足时仍输出样本数和最大值。

**窗口。** 默认统计窗口为一个实际采样周期。连续超限判断保留各周期的判定结果，不平均多个 P99。需要更长分布窗口时应合并桶计数后重新求 P99，不能对 P99 求平均。

**并发与开销。** 高频路径使用本地计数或短临界区更新，周期读取时短暂复制/切换统计区，不能与清空操作竞争而丢计数。仅对配置的 vCPU 和 IRQ 收集时延分布；原始 trace 仅在诊断时按需启用。

## 4. 各监控项详细设计

### 4.1 CPU-1：调度延迟 / 就绪等待时间

#### 监控指标

| 指标 | 含义 |
|---|---|
| `ready_total_ns` | 原有累计 READY 等待时间 |
| `ready_delta_ns` | 当前周期累计等待时间增量 |
| `ready_wait_max_ns` | 本周期已完成单次等待的最大值 |
| `ready_wait_p99_ns` | 本周期已完成单次等待的 P99 估计值 |
| `current_ready_age_ns` | 当前仍为 READY 的 vCPU 已等待时间 |

本项将“调度延迟”明确为 **vCPU 进入可调度 READY 状态，到被选中投入运行之间的等待**。它不等于整个上下文切换耗时，也不包含未观测到的 Guest 首条指令执行时间。

#### 采集与统计

周期读取 `vmm_scheduler_stats()` 获取已有累计量。为了获得单次分布，在 READY 等待开始时记录时间，在 READY→RUNNING 的调度点结算一次等待；运行中被抢占后重新 READY 也构成新的等待区间。

```text
单次等待 = 获得运行时的 HOS 时间戳 − 本次进入 READY 的时间戳
```

迁移过程中仍处于同一次 READY 等待时保留起点，不因换核清零。暂停、reset 等终止等待时清理记录，不把暂停时间作为正常调度等待。尚未得到运行的对象另外检查 `current_ready_age_ns`，避免长时间饥饿没有完成样本而被遗漏。

**纠错：`ready_nsecs` 的周期差值只能反映周期内累计等待，不能作为每次等待样本计算 P99。历史 `ready_max_nsecs` 也不能直接当作本周期峰值反复报警。** 如果暂不增加埋点，就先提供累计值和已确认含义的历史峰值，并将 P99 标记为“未实现”。

#### 阈值与处置

采样周期默认 1 s。`sched_latency_p99_ns`、`ready_age_limit_ns` 按板端负载和调度目标配置，原稿未给出的阈值不直接填入未经测量的产品默认值。超限默认记录并上报，不直接降优先级或 reset；降优先级可能进一步加重等待。

### 4.2 CPU-2：一键采集 / 异常指令周期识别

`monitor collect [guest_id]` 输出一次现有统计快照，内容包括 pCPU 利用率、vCPU 状态和等待、Guest CPU 占用率、trap/exit 类型及速率、IRQ 延迟、内存水位和最近告警。通过内部读取接口汇总，不直接串行执行大量交互命令，也不默认打开全量 trace。

对陷入类型提供次数、速率和占比；能够取得处理时延的路径再显示最大耗时。某类陷入明显超阈值时，标记为“高频陷入”或“陷入处理耗时异常”，而不是直接命名为“指令周期异常”。同一份 exit 计数与 4.8 复用。

**能力边界：现有 trap 计数不能推导任意 Guest 指令的实际执行周期。** 附件中的 ftrace 有函数入口记录，而函数退出钩子为空 [2]，也不能直接用作完整的单条指令计时。本版保留一键诊断，将“精确异常指令周期识别”列为保留问题；确需该能力时另行确认可用的性能计数或跟踪支持，不纳入常驻必选功能。

诊断命令默认读取已缓存快照。需要“本次测量”的用户可执行带时长的采集操作，等待发生在命令线程，不占住 monitor_agent 的周期循环；报告注明采样时间和跨度。

### 4.3 CPU-3：域 CPU 占用率与限流

#### HOS 采集内容

从每个 Guest 的 vCPU 累计运行时间计算：

```text
vCPU 运行占比 = Δrunning_ns / Δtime_ns × 100%
Guest CPU 占用 = Σ该 Guest 各 vCPU 的 Δrunning_ns / Δtime_ns × 100%
```

本版采用“一个物理核时间当量为 100%”的显示口径：Guest 为 200% 表示周期内累计使用约两个核当量。阈值采用同一口径，不能把多 vCPU Guest 的 100% 误当成整台 Guest 满载。

例如，一个 Guest 在 1 s 内的各 vCPU 运行时间合计为 2.4 s，则显示为 240%。可另显示“每 vCPU 平均占比”，但需明确分母；它不一定代表 Guest 可获得物理算力的利用率。

pCPU 利用率按已有 idle 采样接口的真实口径计算。接入前确认 running、system、irq 的包含关系，不把已经计入的时间重复相加。HOS 统计的是调度运行时间，不等于 Guest 内部进程消耗，也不保证与 Guest 的 `top` 数值相同。

#### 阈值与处置

默认每 1 s 更新一次。CPU 高占用阈值按 Guest 分别配置，可使用连续 3 个有效周期超限作为告警条件。高占用本身只表示负载高，默认记录并上报，不直接认定 Guest 死机。

#### 限流边界

Guest 内进程或线程级限流由 Linux/Android 实现。HOS 只能针对自身调度的 vCPU/Guest 资源进行控制。

原稿提出的降优先级、缩短时间片属于调度参数调整，**不能保证严格 CPU 配额上限**。真正的 quota/period 限流还需调度器实现预算记账、预算耗尽后的执行限制及周期补充，不能由 1 s 监控线程简单完成。

因此本版先实现占用率采集、阈值与上报，保留 HOS 限流动作接口；若当前调度器没有已验证的资源限制能力，则标记为“保留，未实现”。不为轻量监控重设计调度算法。

### 4.4 内存-1：Guest 物理内存用量 / 内存水位

#### 监控口径

| 指标 | 数据来源 | 含义与限制 |
|---|---|---|
| Guest 分配内存 | HOS 的 Guest region 配置 | 分配给 Guest 的容量，不代表 Guest OS 已使用容量 |
| HOS RAM 总量、已分配量、剩余量 | `vmm_host_ram_*` | 对应 HOS RAM 管理器的统计范围，不代表 Guest 可用内存 |
| HOS DMA heap 剩余量与剩余比例 | `vmm_dma_heap_free_size()` 加池总容量 | HOS 自身的连续内存池 |
| Guest 实际内存使用量 | Linux/Android 内部统计 | HOS 不直接采集；需要时由 Guest 侧补充 |
| Guest CMA 总量和剩余量 | Linux/Android 内部统计 | 不用 HOS DMA heap 代替 |

Guest 分配量在初始化和布局变化后更新，不必每秒遍历全部 region。应只统计约定的 RAM 类型，区分独占与共享区域；重复映射、别名区域不能重复计入同一物理分配量。若当前接口无法可靠去重，则分别展示 region 容量，不给出误导性的跨 Guest 总和。

**纠错：在 region_add/region_del 处计数只能描述分配或映射变化，不能据此得到 Guest 运行时内存使用量。HOS 没有 CMA，不代表 Guest 内核没有 CMA，也不需要为了本监控在 HOS 新增 CMA 分配器。**

#### 采样、阈值与处置

HOS RAM 和 DMA heap 默认每 1 s 采样。统一采用“剩余比例”口径：

```text
剩余比例 = 剩余容量 / 对应池总容量 × 100%
```

对运行期可分配的内存池，建议初始预警线为剩余比例低于 10%，连续 3 个周期；这是待板端验证的工程初值。Guest 静态划分本身可能正常占用大部分 host RAM，因此全局 RAM 阈值需结合剩余可分配资源单独设定，不能把静态高占用直接判为内存压力。内存分配失败另做事件计数并立即记录，不能只看百分比。只有剩余量而没有可信总容量时，显示绝对剩余值，比例为“不可用”。

动作以告警、事件记录和上报为主。不由监控器主动释放正在使用的内存，不自动回收静态分配给 Guest 的内存。Guest 内部内存压力处置仍由 Guest 完成。

### 4.5 内存-2：各域内存及共享内存边界异常

在原有校验明确判定异常的分支增加计数，保留原有错误返回或异常处理：`vmm_shmem_read/write/set()` 的越界拒绝、非法 region 重叠、非法地址或权限访问，以及能够归属到具体设备/域的 IOMMU fault。

事件记录至少包含发生时间、Guest ID（无法归属时标为未知）、异常类型、访问地址/长度及相关错误码。高频重复异常只累计次数，首条和周期摘要进入告警缓存，避免打印风暴。

**不是所有 Stage-2 fault 都是越界。** 应先沿现有逻辑区分合法的设备模拟访问和真正非法访问，不能在统一异常入口将所有 fault 都计为内存破坏。同一异常在多个函数传播时只在约定位置计一次。

首次确认的非法访问即记录，重复发生时按 1 s 汇总频率；无需等待达到很高比例才记录。原有拒绝访问是主要保护措施，监控负责补充告警和上报，不改变原有访问许可。

保留边界：已经合法映射的 Guest 内部地址被错误写入、没有经过上述检查接口的共享内存访问，HOS 不一定能够发现；没有接入 HOS 的 DMA fault 来源也不能统计。此项命名为“可观测边界异常”，不宣称检测全部内存越界。

### 4.6 内存-3：IOMMU 页表请求异常比例与限流

#### 采集位置与分类

在 HOS 实际可观测的 IOMMU 请求入口增加计数，优先选择已有有效性校验所在层。不得同时在 `vmm_iommu_map/unmap()` 和其底层 SMMU 实现对同一请求重复计数。

| 计数 | 定义 |
|---|---|
| `request_total` | 同一统计入口收到的 map/unmap 请求数 |
| `invalid_request` | 上述请求中，经现有规则确认参数、范围或权限非法的请求数 |
| `map_fail` / `unmap_fail` | 操作失败或未达到预期解除范围的次数，按当前接口语义判定 |
| `dma_fault` | SMMU 报告的设备 DMA fault 次数，单独统计 |

重复 map 是否非法，以当前接口约定和检查结果为准。资源不足等操作失败单独记录，不全部混为非法请求。

#### 阈值

```text
非法请求比例 = Δinvalid_request / Δrequest_total × 100%
```

保留原稿的 **超过 20%**。建议每 1 s 计算，窗口总请求数至少为 100 才进行比例判断，连续 3 个有效周期超限后告警；最小请求数和持续周期均可配置。请求少时仍记录具体非法请求，不把“样本不足”解释为没有异常。

**DMA fault 不属于本次 map/unmap 请求的失败子集，不能用 DMA fault 数除以 map/unmap 请求数表示异常请求比例。** 无法按 Guest 归属时先按 IOMMU domain 或设备统计，不猜测 Guest 身份。

#### 处置与保留问题

本版实现计数、比例告警和上报。只有在请求入口能够安全拒绝/返回限流错误、且调用方能正确处理时，才接入请求限流。

不能在底层 map/unmap 路径任意睡眠或静默丢弃请求，尤其不能丢弃必要的 unmap 后继续复用内存。当前缺少入口和调用方约定时，限流明确标记为“保留，未实现”。日志限频不等于 IOMMU 请求限流。

### 4.7 中断-1：端到端延迟 P99 / 抖动

#### 明确测量区间

沿用原稿四个检查点的命名，但区分测量含义 [1]：

| 时间点 | 含义 | HOS 能否独立取得 |
|---|---|---|
| `t0: trigger` | 设备中断产生或受控测试触发 | 一般需要设备时间戳或测试埋点，不能默认存在 |
| `t1: hos_ack` | HOS 已接收/确认中断的约定检查点 | 可在 HOS 中取得，具体放置位置需核对 |
| `t2: lr_write` | 对应虚拟 IRQ 写入 LR 的检查点 | 经该虚拟化路径投递时可取得 |
| `t3: gos_ack` | Guest 接收/处理中断的检查点 | 需要 Guest 或受控测试配合 |

本版常驻监控优先提供：

```text
HOS 内部 IRQ 投递延迟 = t2 − t1
```

具备对应时间戳时才另外提供 `t2−t0`（触发至注入）或 `t3−t0`（触发至 Guest 响应）。不把缺失的起点或终点补成 0，也不把内部延迟重新命名为完整端到端延迟。

`set_lr/clear_lr` 间隔和 `t2−t1` 不是同一指标，只能按其实际含义单独展示。LR 写入也不等于 Guest 已完成设备中断处理。

#### 采样与统计

只对配置的关键 IRQ 采集时间戳。必须能够将起止点对应到同一次可观测投递；遇到合并、起点丢失或路径不经过对应终点时，不构造错误样本，记录未配对数量即可。

每 1 s 汇总有效样本，输出 count、avg、max、P99 估计值。抖动统一采用 `max−min`，避免混用 `max−P99` 和标准差而使阈值含义不一致。需要其他定义时另设指标名，不增加为默认必选统计。

#### 阈值与处置

`sched` 与 `irq` 的时延阈值分别配置；`irq_hos_p99_ns` 的值按板端路径测量确定。缺少阈值时仍显示统计，但不启用该阈值告警。超限记录 Guest、IRQ、延迟及样本数并上报，不直接屏蔽正常中断。

暂时无法配对的 IRQ 保留计数和已具备的 pending 信息；完整端到端测量列为条件支持项。

### 4.8 中断-2：exit 陷入风暴

周期读取每个 vCPU 的 trap/exit 累计计数，按类型和总量计算速率：

```text
exit_rate = Δexit_count / 实际采样间隔秒数
```

使用一层互斥分类统计总量；不能将某个总计数与其子项再次相加。原稿的异常类别计数与 SGI/PPI/SPI/LPI 计数应先核对含义，不能未经确认全部混为同一种 Guest exit。

保留原稿条件：**每个 vCPU 的 exit 速率超过 50,000 次/s，连续 5 s。** 默认由连续 5 个有效的约 1 s 观察区间判断，各区间均需超限。采样明显迟到或中间没有有效数据时，只能报告实际跨度平均率，重新建立连续判断，不能推定缺失期间一直超限。

触发时保存 vCPU/Guest 标识、主要 exit 类型、当前速率、CPU 占用率和等待情况，写入事件并上报。一次风暴期间重复事件合并，保留总次数及最大值。

**纠错：高 exit 速率是性能异常线索，不等于 HOS 已死机；“保存死机现场类日志”也不要求主动调用 `vmm_panic()`。** 本版默认保存异常快照，不主动制造整机崩溃。vCPU/Guest reset 仅在目标、作用范围及现有接口适用性明确后由可选动作执行；未确认时只上报，不自动重置。

### 4.9 中断-3：异常注入 / 疑似丢失中断

#### 可直接监控的内容

| 项目 | 判断依据 | 输出名称 |
|---|---|---|
| 非法或失败的注入 | 已有校验/注入接口的错误分支 | 注入错误 |
| 某 IRQ 占比异常高 | 同一统计范围内的计数与绝对速率 | IRQ 来源高占比 / 风暴候选 |
| IRQ 长时间待投递 | pending 状态、起点、目标 vCPU 状态 | pending 超时 / 疑似投递卡滞 |
| LR 资源不足 | 原有 overflow/underflow 相关计数 | LR 资源不足 |

默认按同一 Guest、同一“虚拟注入计数层”计算：

```text
IRQ 来源占比 = 某 IRQ 的本周期计数 / 该 Guest 全部 IRQ 的同层计数 × 100%
```

物理 IRQ 来源统计可以作为诊断项另外显示，但不混入上述分母。

保留原稿的 **超过 80%、持续 5 s**，同时要求达到配置的绝对速率下限。否则总共只有几次中断时也可能达到 100%，不能据此认定风暴。绝对速率下限未标定时先显示占比，不启用风暴处置。

#### pending 超时与丢失判断

可复用原稿提到的虚拟 IRQ 超时能力；700 ms 仅作为原有候选值保留，接入时核对其语义 [1]。判断需考虑 Guest 正常暂停、相关 IRQ 被有意屏蔽等情况；LR 暂时不足也不直接表示中断丢失。

**`assert_count−execute_count` 只能先作为待处理差额，不能直接定义为丢失数。** 重复置位、合并、屏蔽和不同计数位置都可能导致差额。没有事件对应关系和确认条件时，输出“疑似投递卡滞”，真正丢失中断检测列为保留问题。

周期扫描也存在检测延后：若每 1 s 检查一次、年龄阈值为 700 ms，发现时间可能比 700 ms 再晚约一个采样周期，且受调度影响。不能宣称“必定在 700 ms 内发现”。确有更短发现时限时，单独缩短该检查间隔或复用已有超时检测，不强制所有监控项高频采集。

#### 处置

默认记录、汇总并上报。屏蔽只允许用于已确认可单独控制且允许中断业务的 IRQ，不自动屏蔽定时器、IPI、共享物理中断等关键来源。由既有 IRQ 管理接口实施，记录目标和结果；不能因为屏蔽后计数下降就自动判定源端恢复正常。

vCPU 重置不作为单一计数差或 80% 占比的默认动作。`cpu_vcpu_inject_undef/pabt/dabt()` 等异常注入函数也不是设备 IRQ 的限流或屏蔽接口，不用于代替这些动作。

## 5. 配置项设计

### 5.1 编译期与运行时配置

保留原稿的 openconf 开关与设备树配置方式。建议使用 `CONFIG_MONITOR_AGENT` 控制是否编译，使用 `CONFIG_MONITOR_AGENT_PERIOD` 提供默认采样周期。关闭编译时，新增热路径记录入口为空操作。

运行时由设备树 `/monitor_agent` 节点提供板级参数，`monitor config` 命令允许修改已支持的配置。优先级为：运行时命令 > 设备树 > 编译默认值。命令修改只影响本次运行，不额外设计持久化配置服务。

### 5.2 主要参数

“待标定”表示采集仍可运行，但相关阈值告警尚未启用，不表示测量值为 0。

| 参数 | 含义 | 本版默认或建议 |
|---|---|---|
| `sample_period_ms` | 常规采样与汇总周期 | 1000 ms，沿用原稿 |
| `p99_min_samples` | 输出 P99 所需最少有效样本 | 100，工程初值 |
| `alarm_consecutive` | 普通性能指标连续超限次数 | 3，工程初值；不替代 exit/IRQ 专有 5 s 条件 |
| `alarm_repeat_ms` | 同一持续告警摘要输出间隔 | 30000 ms，工程初值 |
| `sched_latency_p99_ns` | 单次 READY 等待 P99 阈值 | 待标定 |
| `ready_age_limit_ns` | 当前 READY 等待年龄阈值 | 待标定 |
| `cpu_usage_pct` | Guest CPU 高占用线，单核当量为 100% | 按 Guest 标定，可大于 100 |
| `mem_low_free_pct` | HOS 运行期内存池剩余比例预警线 | 10%，工程初值；全局 RAM 须结合静态划分另行配置 |
| `iommu_abnormal_pct` | 非法 IOMMU 请求比例阈值 | 20%，保留原稿 |
| `iommu_min_requests` | IOMMU 比例判断最小请求数 | 100/统计窗口，工程初值 |
| `irq_hos_p99_ns` | HOS 内部 IRQ 投递延迟 P99 阈值 | 待标定 |
| `irq_e2e_p99_ns` | 完整指定端点的 IRQ 延迟 P99 阈值 | 仅完整测量具备后配置，不能用于内部延迟冒名告警 |
| `exit_storm_rate_ps` | exit 风暴速率阈值 | 50000 次/s，保留原稿 |
| `exit_storm_window_s` | exit 连续超限时间 | 5 s，保留原稿 |
| `irq_abnormal_pct` | 单一 IRQ 来源占比阈值 | 80%，保留原稿 |
| `irq_abnormal_window_s` | IRQ 来源异常持续时间 | 5 s，保留原稿 |
| `irq_storm_min_rate_ps` | IRQ 风暴判断的来源绝对速率下限 | 待标定；未配置时只显示占比 |
| `virq_pending_timeout_ms` | 虚拟 IRQ 待处理年龄阈值 | 原稿候选 700 ms，复核后采用 |
| `event_capacity` | 本地事件缓存条数 | 128，工程初值 |
| `action_policy` | 各监控项允许动作 | 默认 log；report 接通后开启；其他默认关闭 |

20%、80%、5 万次/s 是原稿需求值，不代表已通过当前板端业务标定。新增的 1 s 周期外辅助参数、10% 水位等也应通过正常负载测试调整。

配置时检查周期非零、时间单位一致、事件容量在编译上限内。比例通常限制在 0～100%，但单核当量口径的 `cpu_usage_pct` 例外。更改阈值或采样周期后重新建立连续超限计数，避免沿用旧规则的累计结果。

### 5.3 简单告警规则

普通性能指标默认连续超限后告警；确认的非法访问或注入错误在发生路径即时计数并保留必要信息，由周期线程输出，不在高频路径逐条打印。

同一“指标 + 对象”首次触发时输出，持续期间累计次数和峰值，按 `alarm_repeat_ms` 输出摘要；连续若干正常有效周期后记录一次恢复。恢复只表示该监控条件不再成立，不宣称 Guest 所有功能正常。无有效数据时暂停判断，不能作为恢复依据。

## 6. 命令设计

保留原稿命令形式，Guest 参数使用 ID；名字可以作为便捷显示或查找方式，但内部不依赖名称判断 Linux/Android 身份。

```text
monitor start | stop | status
monitor collect [guest_id] [duration_ms]
monitor cpu [guest_id]
monitor mem [guest_id]
monitor irq [guest_id]
monitor events [count]
monitor config show
monitor config set <key> <value> [guest_id]
monitor action <log|report|throttle|mask|reset> <on|off>
```

| 命令 | 主要输出或行为 |
|---|---|
| `status` | 运行状态、采样周期、最后采样时间、已启用项目、事件丢失及上报失败计数 |
| `collect` | 一次汇总报告；指定时长时给出该观察跨度内的数据，不阻塞监控线程 |
| `cpu` | vCPU 状态、运行占比、累计等待、窗口最大值、P99/当前等待年龄及有效性 |
| `mem` | Guest 分配内存、HOS RAM/DMA heap、边界和 IOMMU 统计 |
| `irq` | 已支持路径的延迟、exit 速率、IRQ 来源占比、pending 超时及注入错误 |
| `events` | 最近异常和已尝试动作的结果 |
| `action` | 仅启用当前实现支持且目标范围明确的动作；不执行一次即时 reset |

每项结果注明单位和时间范围。至少区分“有效”“样本不足/读取失败”“未实现或本层不可获取”，不把缺失数据填成 0，也不让历史峰值冒充本周期峰值。

`stop` 只停止监控采集和判断，不关闭原有内存检查、异常处理或 IRQ 保护逻辑。已经执行的屏蔽等动作按原有管理方式恢复，不因停止监控而擅自撤销保护。

## 7. 上报机制与异常处置

### 7.1 本地事件与日志

HOS 使用固定容量事件缓存，不依赖 ServiceVM 是否在线。事件内容保持简洁：

| 字段组 | 内容 |
|---|---|
| 时间和类型 | HOS 单调时间戳、事件序号、监控项编号、告警级别 |
| 关联对象 | Guest/vCPU/pCPU/IRQ 或设备标识；无归属时明确标记 |
| 观测结果 | 当前值、阈值、统计时间跨度、重复次数 |
| 动作结果 | 已尝试的动作、返回值；未尝试或不支持也明确记录 |

事件是 HOS 内部结构，外部传输结构在接口接入时固定版本、字段宽度和单位。日志以首条和摘要为主；缓存满时丢弃最旧事件并增加丢失计数，不等待消费者腾出空间。

需要保存更多现场信息时，复用原有调度、trap、IRQ 及内存快照。`vmm_logsystem_dump_to_file()` 仅在确认文件路径和调用上下文可用时执行，失败不阻塞监控。保存异常信息不要求调用 `vmm_panic()`。

### 7.2 向 ServiceVM 提供事件

本版选用原稿允许的 hypercall 方式作为最小接入方案：**HOS 本地事件缓存 + 一个拟议的只读 `HC_MONITOR_READ` 接口。** ServiceVM 周期读取事件，完成日志汇聚、显示或后续业务处置。该接口尚未出现在附件中，编号和参数布局需与当前调用表对齐，不能按现有 ID 猜测分配 [2][5]。

```text
HOS 检测异常 → 写入本地事件缓存
ServiceVM 定期调用 HC_MONITOR_READ → HOS 返回一批有界事件
ServiceVM 保存/处理事件；需要时自行转发给 Android
```

只约定读取游标、最大条数和返回缓冲区，不设计复杂订阅或确认协议。HOS 根据调用者身份控制读取权限；检查缓冲区长度和 Guest 地址范围，按固定批量上限复制。游标落后于已覆盖事件时明确返回丢失信息。

这条路径由 ServiceVM 拉取完成事件交付，不应描述为当前已经存在的主动推送。若要求异常发生后主动提示，再接入下一节的通知方式。ServiceVM 未读取、尚未接入或读取失败，不影响 HOS 本地记录和原有保护逻辑。

### 7.3 CCM/sbuf 的可选复用

附件中的 `ccm_upcall()` 目前面向 ServiceVM，且已经用于 I/O 通知 [2][3]。在 ServiceVM 能够分辨并正确处理健康事件之前，不能单独增加同一中断注入就宣称完成健康上报。

后续可在事件入缓存后发送一次 CCM 提示，由 ServiceVM 再调用只读接口取数；也可将 `VMMU_VM_EVENT` 作为等价的事件承载方式，但需先确认它的现有使用方、格式和缓存一致性约定。两种数据承载方式不必同时实现。

本次不修改 `io_request` 和现有 asyncio 数据格式，不把健康记录写入正常 I/O 队列；也不要求 Android 新增接收接口。直接上报 Android 若以后成为要求，再列入接口扩展。

### 7.4 异常动作

| 动作 | 默认状态 | 执行规则 |
|---|---|---|
| 记录日志/事件 | 开启 | 固定缓存、限频摘要，任何 Guest 状态下都不等待其接收 |
| 保存异常快照 | 开启基础信息 | 额外耗时导出交给既有工作队列，不在采集埋点中进行 |
| 上报 ServiceVM | 接通后开启 | 先保证可读取，主动通知为可选项 |
| 保留原有非法访问拒绝 | 原机制照常执行 | monitor_agent 只补充记录，不改变校验结论 |
| CPU 限流 | 默认关闭、当前保留 | 仅调用已具备并验证的调度限制能力；降优先级不冒充严格配额 |
| IOMMU 请求限流 | 默认关闭、当前保留 | 入口、返回值及调用方行为明确后接入；不得静默丢弃必要操作 |
| IRQ 屏蔽 | 默认关闭、条件支持 | 指定允许目标，使用已有接口；关键或共享中断禁止自动屏蔽 |
| vCPU/Guest 重置 | 默认关闭、条件支持 | 使用已验证的管理入口；动作目标和作用范围明确，同一告警不重复提交 |
| `panic` / 整板复位 | 不作为本监控默认策略 | 仍由原有不可恢复错误处理或明确的平台策略决定 |

记录动作时区分“调用已接受”和“动作实际完成”。附件中 `hcall_reset_vm()` 可以异步返回 [2]，因此仅凭返回成功不能记为“已恢复”。没有合适的完成依据时记录“已提交，结果未确认”，不为此增加 DSM 恢复监控模块。

操作接口未核对、目标不允许或能力未实现时，仅记录和上报异常，不试探性执行破坏性动作。

## 8. 实施阶段划分

### 8.1 实施步骤

保留原稿 P0～P3 分阶段方式，先完成可独立运行的本地功能，再对接外部上报与条件支持项。

| 阶段 | 主要内容 | 交付结果 |
|---|---|---|
| P0：基础骨架 | 周期线程、配置、命令、固定事件缓存；CPU/内存已有累计统计 | 本地能启停、查询、记录；不依赖 Guest 接收端 |
| P1：CPU 与 IRQ | 单次调度等待统计、Guest CPU 占用、exit 连续超限、关键 IRQ 延迟和可见异常 | 原稿 CPU/中断项中 HOS 可观测部分形成告警 |
| P2：内存与诊断 | 边界异常计数、IOMMU 请求分类、水位告警、一键快照 | 原稿内存项中 HOS 可观测部分及诊断报告 |
| P3：上报与可选动作 | 对接 ServiceVM 只读事件接口；按实际能力接入通知、屏蔽或重置 | 事件可交付；仅启用明确支持的动作 |

严格 CPU 配额、IOMMU 请求限流等保留项不强制包含在 P3 中，也不能因为预留了接口就计为已完成。

### 8.2 基本验证

以下为测试设计，不是已执行结果；不扩展为复杂的系统认证测试体系。

| 测试项 | 基本方法与通过要求 |
|---|---|
| 启停与采样 | 验证周期采样、停止和重新建基线；参数变化后不产生虚假速率 |
| 调度等待 | 构造已知单次等待分布，核对分桶 P99、窗口最大值及未完成等待年龄；不以累计差值代替分布 |
| CPU 占用率 | 用可控 vCPU 负载验证单核 100%、多核可超过 100% 的统计口径 |
| 一键诊断 | 有效数据注明跨度；未实现的指令周期、缺失统计不输出伪造结果 |
| 内存水位 | 降低测试池剩余量触发告警；明确区分 Guest 分配量、HOS DMA heap 和 Guest CMA |
| 边界异常 | 构造可控非法访问，原路径仍拒绝；正常 MMIO 模拟不误记为越界 |
| IOMMU 比例 | 在已知请求集合中验证分子分母、20% 边界及最小样本数；DMA fault 单列 |
| IRQ 延迟 | 对可配对测试中断核对时间戳；缺起点/终点不生成错误延迟样本 |
| exit 与 IRQ 占比 | 分别验证超过/等于阈值、持续不足 5 s、连续满足 5 s 及采样缺失情况 |
| pending 与动作 | 正常暂停/屏蔽不直接判丢失；未允许的屏蔽或 reset 不执行 |
| 上报与缓存 | ServiceVM 未接入、读取变慢和事件缓存满时，HOS 不阻塞，丢失信息可见 |
| 监控开销 | 对比关闭/开启后的 CPU 开销、调度最大等待及 IRQ 延迟；以板端结果决定桶数、对象范围和周期 |

对严格配额、完整端到端延迟和真正丢失中断检测，必须分别满足对应前置条件后再验收，不能用已有告警替代其功能验收。

## 9. 关键复用清单与新增接口

### 9.1 关键复用入口

不重复写入可能随版本变化的行号，按文件和函数定位。下表中来自原稿而未提供源码的签名，在编码前以当前工程头文件为准。

| 范围 | 复用入口 | 使用说明 |
|---|---|---|
| 周期线程 | `vmm_threads_create/start/stop()` | 运行 monitor_agent；必要时使用已有 affinity 接口 |
| 时间 | `vmm_timer_timestamp()` | 统一 HOS 内部时间基准，核对单位 |
| 调度统计 | `vmm_scheduler_stats()`、`vmm_scheduler_idle_time/irq_time()` | 封装后读取；不能未经核对假定所有输出均是累计量 |
| trap | `vmm_manager_vcpu_get_trap_cnt()` | 读取统一计数；不由监控擅自 clear |
| RAM / DMA heap | `vmm_host_ram_total_used/free_frames()`、`vmm_dma_heap_free_size()` | 使用原有分配器统计 |
| Guest 内存 | region 遍历或布局查询接口 | 统计分配信息，不调用带映射副作用的 hcall 作为周期读接口 |
| IOMMU | `vmm_iommu_map/unmap()`、SMMU fault 路径 | 选定计数层，保持原行为 |
| IRQ | host IRQ、VGIC 关键点及 `vmm_manager_detect_virq_timeout()` | 复用有效时间点和已有检测逻辑 |
| 通知 | `ccm_upcall()`、`vmm_devemu_emulate_irq2()` | 仅在 Guest 端识别健康提示后接入 |
| 日志 / 管理 | logsystem、现有 IRQ/vCPU/Guest 管理接口 | 在适当线程上下文执行，保留返回结果 |
| 命令 / 配置 | `vmm_cmdmgr_register_cmd()`、devtree/openconf | 沿用工程原有接入方式 |

### 9.2 最少数据结构

新增数据优先放在 monitor_agent 私有结构中，仅在热路径确有必要时给原对象附加少量记录字段，不为统计 Guest CPU 占用强制大幅修改 `struct vmm_guest`。

| 结构（拟议） | 核心内容 |
|---|---|
| `monitor_config` | 采样周期、各项阈值、启用项目和允许动作 |
| `monitor_vcpu_stat` | 上次运行/等待/exit 计数、READY 起点、时延分桶和本周期结果 |
| `monitor_guest_stat` | Guest CPU 聚合值、分配内存信息及归属明确的错误计数 |
| `monitor_irq_stat` | 指定 IRQ 的计数、配对时间戳、时延分布、pending 年龄 |
| `monitor_event` | 简洁事件记录，字段见 7.1 |

所有数组、桶和事件容量均受配置上限约束。开始监控时一次分配；高频记录不动态申请内存。没有对应信息的字段标为不可用，不为填满结构扩展 Guest 内部采集。

### 9.3 新增接口建议

以下均为概要级拟议接口，名称可按 HOS 代码规范调整，不代表附件中已经存在。

| 接口 | 用途与约束 |
|---|---|
| `vmm_monitor_init()` | 读取配置、分配固定统计区并接入命令 |
| `vmm_monitor_start/stop()` | 启停线程和采集开关，重新建立统计基线 |
| `vmm_monitor_record_latency()` | 热路径记录一个已经测得的单次时延；不做 P99 计算和打印 |
| `vmm_monitor_record_error()` | 错误分支计数及保留必要的首条上下文；不得睡眠 |
| `vmm_monitor_get_snapshot()` | 命令读取已有结果，不触发全量 trace |
| `vmm_monitor_read_events()` | 读取有界事件批次，供命令和拟议只读 hypercall 共用 |

周期计算和动作函数保持模块内部使用即可。无需新增通用 provider、订阅管理器或复杂 notifier 链；若已有 notifier 便于复用，只能在允许的线程上下文调用，不能从 IRQ/调度锁内调用可能睡眠的回调。

## 10. 风险与保留问题

### 10.1 保留问题清单

| 编号 | 问题 | 当前处理方式 | 后续需要确认的内容 |
|---|---|---|---|
| R1 | 调度和 IRQ 单次 P99 的采样来源 | 计数先用已有接口；精确分布按必要埋点实现 | 当前调度状态转换点、IRQ 时间戳配对路径 |
| R2 | 精确的异常指令周期识别 | 仅提供陷入计数/已支持耗时的诊断 | 平台可用的指令/性能跟踪能力及实际需求 |
| R3 | Guest 实际内存使用量和 CMA | HOS 不直接采集；明确标记 Guest 侧负责 | 是否需要由 Guest 向外部管理端提供这些指标 |
| R4 | Guest/vCPU 严格 CPU 配额 | 保留限流动作，不把调度参数调整当作配额 | 当前调度器是否已有适用机制及接口 |
| R5 | IOMMU 请求限流 | 先计数、告警、上报 | 可控入口、调用方错误处理及 map/unmap 返回语义 |
| R6 | 完整物理触发至 Guest 响应时延 | 优先提供 HOS 内部延迟 | 物理触发和 Guest 端时间戳是否具备 |
| R7 | 真正丢失中断的确认 | 输出 pending 超时、错误计数和疑似卡滞 | 是否具备事件对应关系及确认条件 |
| R8 | Guest 上报接入 | 本地缓存先实现，拟议只读事件接口 | ServiceVM 接收接口；是否需要主动 CCM 提示 |
| R9 | 自动屏蔽、重置的可用范围 | 默认关闭，不支持时只上报 | 可允许目标、现有接口及结果确认方式 |
| R10 | 阈值和已有统计接口语义 | 已有需求值保留，其余待标定 | 正常/压力负载基线、累计量与窗口量的真实定义 |

DSM 进程状态、DSM 重拉起和 Guest 应用健康检查是 **范围外事项**，不是要求 HOS 后续补齐的保留监控项。

### 10.2 主要风险及控制

**监控开销。** 时延分桶和异常记录必须有界，限制关键 IRQ 范围；先用 1 s 周期，不为了未确认的时限引入高频全量采样。具体 CPU 和内存开销以板端测试为准，不承诺未经测量的百分比。

**误报。** 明确单位、分母、采样跨度和数据有效性；在正常启动、暂停、reset 和计数清零后重建基线。历史峰值、缺失数据及合法模拟异常不直接触发重复告警。

**误处置。** 默认以记录、快照和上报为主；保留原有非法访问拒绝。只对已验证且明确允许的对象执行屏蔽或重置，不因单项性能超阈值使整板停止。

**自身执行边界。** monitor_agent 是 HOS 中的线程；当 HOS 已完全无法调度时，不能保证它还能采集或上报。本文不为此新增独立看门狗子系统，平台已有保护措施保持原样。

### 10.3 设计资料

本文使用以下用户提供材料，未将上游 ACRN/Xvisor 的其他实现当作当前 HOS 已有能力。

[1]《HOS Hypervisor monitor_agent 性能监控代理设计文档》，v0.1。主要对应原稿第 1～10 节的需求、能力盘点、统计方式、动作和接口。

[2] `cpu_hcall.c.txt`。相关函数：`dispatch_hcall()`、`ccm_upcall()`、`hcall_vm_intr_monitor()`、`hcall_get_meminfo()`、`hcall_reset_vm()`、`__cyg_profile_func_enter/exit()`。

[3] `vmm_u_ioreq.c.txt`。相关函数：`init_vm_event()`、`sbuf_setup_common()`、`sbuf_put()`，用于确认已有缓冲和通知基础，不扩展 I/O 恢复设计。

[4] `vmm_u_ioreq.h.txt`。相关定义：`SERVERVM_ID`、`VMMU_VM_EVENT`、`shared_buf`、`io_request`。

[5] `cpu_hcall.h.txt`。相关定义：已有 hypercall 编号与函数声明；本设计新增的 `HC_MONITOR_READ` 仍为拟议接口。

---

**本设计的交付范围：原稿九项监控中 HOS 可观测部分的统计、告警、适用处置及事件上报。不可观测或缺少实现基础的能力显式保留，不以增加 DSM 监控或跨 Guest 恢复系统来补齐。**
