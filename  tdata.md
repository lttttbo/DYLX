# eth0 设备中断测试总结  
## 一、测试范围与统计口径  
本轮测试以 **Android Guest 为参照**：不带 -R 为上行，即 Android→PC；带 -R 为下行，即 PC→Android。测试覆盖 UDP、TCP 上下行，以及通知线程绑核前后的对照。  
**需要区分三个统计口径：**  

| 项目        | 本次使用的口径                                    |
| --------- | ------------------------------------------ |
| 应用吞吐      | iperf3 完整约60秒测试的 receiver 结果               |
| eth0 中断统计 | 脚本约41秒采集窗口内，IRQ 131 的增量                    |
| 主方向包数/IRQ | 上行使用 eth0 TX 包数，下行使用 eth0 RX 包数，除以 IRQ 总增量 |
  
下文统计的是 **eth0 设备 IRQ，不是此前调查中的 IPI1 Function call interrupts**。包数/IRQ 是窗口平均值，不代表每次中断实际处理的报文数量。  
## 二、绑核前后的主要测试结果  
**2.1 吞吐与设备中断统计**  
**以下八组测试中，eth0 IRQ 131 的实际中断增量全部位于 CPU0。**  

| 测试场景 | 状态 | iperf3接收吞吐（Mbps） | 采集时长（s） | eth0 IRQ增量 | 平均IRQ/s | 主方向包数/IRQ |
| ----- | --- | ---------------- | ------- | ---------- | ---------- | --------- |
| UDP上行 | 未绑核 | 603 | 41.310 | 84,713 | 2,050.666 | 24.997 |
| UDP上行 | 绑核后 | 756 | 41.210 | 111,486 | 2,705.314 | 24.997 |
| UDP下行 | 未绑核 | 955 | 41.410 | 588,492 | 14,211.350 | 6.000 |
| UDP下行 | 绑核后 | 955 | 41.310 | 587,086 | 14,211.716 | 6.000 |
| TCP上行 | 未绑核 | 856 | 41.455 | 640,022 | 15,438.958 | 4.768 |
| TCP上行 | 绑核后 | 941 | 41.335 | 665,918 | 16,110.270 | 5.046 |
| TCP下行 | 未绑核 | 941 | 41.455 | 582,419 | 14,049.427 | 5.785 |
| TCP下行 | 绑核后 | 938 | 41.315 | 583,063 | 14,112.623 | 5.758 |
  
**2.2 实际中断亲和性**  

| 项目                         | 未绑核状态    | 绑核后      |
| -------------------------- | -------- | -------- |
| IRQ 131 requested affinity | 0-5      | 0        |
| IRQ 131 effective affinity | CPU0     | CPU0     |
| 采集期间中断实际分布                 | 全部在CPU0  | 全部在CPU0  |
| 采集期间IRQ亲和性变化               | 0次       | 0次       |
| irqbalance                 | inactive | inactive |
  
因此，这八组测试中的“绑核前后”，**并没有改变 eth0 IRQ 的实际执行 CPU**。它们主要反映通知线程布局调整后的性能变化，不能单独用于评价“将 eth0 IRQ 从一个 CPU 移到另一个 CPU”的效果。  
**2.3 主要发现**  
**UDP 上行：吞吐提高，但每包设备中断比例没有变化。**  
绑核前后：  
```
eth0 TX PPS：51,260.639 → 67,623.781，增加约31.92%
eth0 IRQ/s： 2,050.666 →  2,705.314，增加约31.92%

```
两者按几乎相同的比例增长，始终约为：  
```
25个TX报文 / 1次eth0 IRQ

```
这说明绑核后的 IRQ 增加主要与发包数量增加相对应，**没有出现低速状态下每包 eth0 中断异常增多的现象**。  
**UDP 下行：吞吐和设备中断行为基本不变。**  
绑核前后均为约955 Mbps、约14,212 IRQ/s、约6个RX报文/IRQ。当前下行没有表现出与上行同等程度的亲和性敏感性。  
**TCP 上行：吞吐由856 Mbps提高至941 Mbps，但不能将其全部设备中断视为纯TX中断。**  
TCP上行同时存在明显的反向接收流量：未绑核时约37,384 RX PPS，绑核后约41,148 RX PPS。因此，IRQ总数对应的是包含双向流量的测试状态，不能直接套用UDP上行的“约25包/IRQ”比例。  
**TCP 下行：整体保持接近线速。**  
完整测试结果由941 Mbps变为938 Mbps；绑核后主要是第一个10秒区间较低，随后均约941 Mbps。采集窗口内的接收速率和IRQ/s也基本稳定。  
## 三、上下行中断数量的差异  

| 对比项目      | UDP上行   | UDP下行     | 结论                     |
| --------- | ------- | --------- | ---------------------- |
| 未绑核IRQ/s  | 2,050.7 | 14,211.4  | 下行约为上行的6.93倍           |
| 绑核后IRQ/s  | 2,705.3 | 14,211.7  | 下行约为上行的5.25倍           |
| 主方向包数/IRQ | 约25     | 约6        | 下行每包对应的IRQ密度约为上行的4.17倍 |
| 实测吞吐表现    | 更容易降速   | 约955 Mbps | 中断次数更多的下行仍然更快          |
  
**结论：UDP上行低吞吐不能用“eth0设备中断次数过多”解释。**  
上下行确实表现出不同的设备中断比例，但当前数据只能确认这一统计差异，不能单独确定具体的硬件合并参数，也不能据此推导每次NAPI处理量。  
## 四、eth0 IRQ 移至 CPU1 的补测  
补测依据 effective affinity 和 irq_delta.csv 归类：**ioeventfd、vhost在CPU0，eth0 IRQ实际在CPU1**，不是全部CPU0。  

| 配置                | eth0 TX PPS | eth0 IRQ/s | TX包数/IRQ  | 每万TX包对应的IRQ |
| ----------------- | ----------- | ---------- | --------- | ----------- |
| 通知线程CPU0，IRQ CPU0 | 67,623.781  | 2,705.314  | 24.996645 | 400.053682  |
| 通知线程CPU0，IRQ CPU1 | 61,369.611  | 2,455.028  | 24.997518 | 400.039710  |
  
CPU1补测窗口内：  
```
CPU0：新增0次IRQ
CPU1：新增100,337次IRQ

```
这确认了中断确实转移到CPU1。**执行位置变化后，发送速度发生变化，但设备IRQ与发送报文的比例仍基本保持25:1。**  
结合你此前贴出的IRQ CPU1上行低吞吐结果，可以保留“eth0 IRQ亲和性会影响性能”的判断；但本轮存在采集干扰，**不宜将不同测试轮次之间的全部吞吐差额直接量化为纯IRQ跨核开销**。  
## 五、采集脚本对结果的影响  
你补充了相同绑核条件下，带采集与不带采集的对照：  

| 条件        | ioeventfd/vhost布局 | eth0 IRQ | UDP上行receiver |
| --------- | ----------------- | -------- | ------------- |
| 运行原逐秒采集脚本 | CPU0              | CPU0     | 756 Mbps      |
| 不运行采集脚本   | CPU0              | CPU0     | 935 Mbps      |
  
带采集的结果比无采集基线低约：  
\frac{935-756}{935}\times100\%\approx19.1\%  
因此：  
**本批带采集数据适合用于分析采集期间的IRQ数量、CPU分布和IRQ/报文比例，不应直接作为产品无干扰条件下的最大吞吐验收结果。**  
同时，capture_status=OK 只表示脚本未发现其检查范围内的计数或状态异常，不能代表采集对性能没有影响。  
## 六、总结性结论  

| 结论项 | 本次测试支持的结论 |
| ------------------------- | ------------------------------------------------------ |
| eth0是否出现设备中断数量异常 | 未发现。UDP上行不同状态均约25包/IRQ，低速没有伴随每包IRQ异常增加。 |
| 上行慢是否因为设备IRQ比下行多 | 不是。UDP下行IRQ明显更多，但仍达到约955 Mbps。 |
| 通知线程绑核是否有效 | 有效。本批UDP、TCP上行均改善；下行变化很小。 |
| 八组主测试能否独立证明IRQ迁移效果 | 不能。八组中eth0 IRQ始终实际运行在CPU0。 |
| IRQ CPU1补测说明什么 | 中断位置确实改变，发送性能出现差异，但每包设备IRQ比例仍基本不变。亲和性影响不要求中断数量同时异常增加。 |
| 设备IRQ与此前IPI是否可以混为一谈 | 不可以。本次统计的是eth0设备IRQ，不是通知链远程唤醒对应的Function-call IPI。 |
| 能否认定端到端完全无丢包 | 不能。接口drop/error增量均为0，但UDP下行iperf3仍记录约0.064%和0.072%的丢包。 |
| 能否用带脚本的756 Mbps作为绑定后的性能上限 | 不能。无采集脚本时同核配置已达到935 Mbps，采集存在明显干扰。 |
  
**最终结论：本次 eth0 中断测试没有发现设备中断风暴或每包中断异常增加。上行性能问题不能单纯归因于 eth0 IRQ 数量，而应区分通知链的跨核执行、物理网卡 IRQ 的执行位置，以及采集工具的扰动。设备IRQ数量正常，并不代表IRQ亲和性不重要；IRQ亲和性影响性能，也不意味着它必须产生更多设备中断。**  
