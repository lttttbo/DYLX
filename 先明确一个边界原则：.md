先明确一个边界原则：  
**Hypervisor 应监控自身能够准确观测并能够归责到 Guest、vCPU、设备或 IRQ 的指标。** Guest OS 内部具有操作系统语义的指标，例如进程 CPU 占用、MemAvailable、CMA 水位、页缓存等，不适合由 Hypervisor 直接推断，应由 Linux/Android Agent 采集后上报。  
另外，“监控周期”需要区分两层：  
* **事件采集周期**：调度、Exit、IRQ、IOMMU 映射等事件发生时立即更新计数器或直方图；  
* **阈值判定周期**：monitor_agent 每 100 ms、1 s 或 5 s 汇总并判断是否持续超限。  
高频路径不能每次打印日志、申请内存或写文件，只应更新时间戳、计数器和固定分桶。  
   
⸻  
   
## 一、监控职责划分  

| 监控项                     | Hypervisor 是否适合监控   | 建议负责方                    |
| ----------------------- | ------------------- | ------------------------ |
| vCPU 调度延时               | 适合                  | Hypervisor               |
| vCPU READY 队列等待时间       | 适合                  | Hypervisor               |
| vCPU、Guest 域级 CPU 占用    | 适合                  | Hypervisor               |
| Guest 内进程、线程、UID CPU 占用 | 不适合直接监控             | Linux/Android Agent      |
| 异常指令、Trap/Exit 原因及耗时    | 适合                  | Hypervisor               |
| Guest 配置物理内存、映射区域       | 适合                  | Hypervisor               |
| Guest 内实际已用内存、可回收内存     | 不适合直接判断             | Linux/Android Agent      |
| CMA 水位及碎片化              | 不适合                 | Linux/Android Agent      |
| Stage-2、共享内存边界异常        | 适合                  | Hypervisor               |
| IOMMU 映射请求、错误与 fault    | 适合                  | Hypervisor               |
| 物理 IRQ 软件入口到虚拟注入延时      | 适合                  | Hypervisor               |
| 外设拉高中断线到 EL2 的硬件传播延时    | 仅靠软件不适合             | 硬件时间戳或外部测试               |
| 虚拟 IRQ 注入到 Guest 中断处理完成 | Hypervisor 单独无法完整判断 | Hypervisor + Guest Agent |
| Exit 陷入风暴               | 适合                  | Hypervisor               |
| IRQ 注入失败、pending 超时     | 适合                  | Hypervisor               |
| 真正意义上的“中断丢失”            | 需联合判断               | Hypervisor + Guest/设备状态  |
  
   
⸻  
   
## 二、CPU 监控项细化  
## 2.1 vCPU 调度延时  
**监控主体**  
由 Hypervisor 监控。  
按照当前 A/B/C 模型：  
* A：Timer Tick 进入 Hypervisor；  
* B：调度选择完成，并配置下一次调度 Timer；  
* C：vCPU 上下文切换完成，Guest 即将执行。  
分别统计：  
T_{AB}=B-A  
T_{BC}=C-B  
T_{AC}=C-A  
其中：  
* A→B 可按当前工程口径称为中断及调度前处理延时；  
* B→C 为 vCPU 切换延时；  
* A→C 为完整调度延时。  
**采集周期**  

| 项目            | 周期            |
| ------------- | ------------- |
| 时间戳采集         | 每次调度 Timer 事件 |
| 直方图更新         | 每次有效样本        |
| P99、平均值、最大值汇总 | 1 s           |
| 阈值判定          | 5 s 滑动窗口      |
| 长期趋势保存        | 1 min         |
| 诊断历史保留        | 最近 60 s       |
  
**统计对象**  
必须至少按以下维度统计：  
* 每个 hCPU；  
* 每个 vCPU；  
* 每个 Guest；  
* 全局汇总。  
不能只保留全局平均值，否则一个 vCPU 或一个物理核发生劣化时可能被其他核的数据掩盖。  
**初始阈值建议**  
根据当前约 1～4 μs 的基线数据，可先采用：  

| 指标            | 预警              | 严重           | 单次硬阈值  |
| ------------- | --------------- | ------------ | ------ |
| A→C 调度延时 P99  | >5 μs，连续3个1 s窗口 | >10 μs，连续5 s | >50 μs |
| A→B 延时 P99    | >3 μs，连续3个窗口    | >6 μs，连续5 s  | >30 μs |
| B→C vCPU切换P99 | >1.5 μs，连续3个窗口  | >3 μs，连续5 s  | >10 μs |
  
这些值适合作为初版阈值，不应直接作为最终安全阈值。正式版本应根据 24～48 小时实测 P99、P99.9 和最大值进行固化。  
建议最终采用：  
```
预警阈值 = max（固定最小阈值，基线P99 × 2）
严重阈值 = max（固定严重阈值，基线P99 × 4）

```
   
⸻  
   
## 2.2 vCPU 就绪队列等待时间  
**监控主体**  
由 Hypervisor 监控。  
**指标定义**  
vCPU 进入 READY 时记录：  
```
ready_enter_timestamp

```
vCPU 被选中并变为 RUNNING 时记录：  
```
ready_wait = running_timestamp - ready_enter_timestamp

```
需要统计：  
* 平均等待时间；  
* 最大等待时间；  
* P95、P99、P99.9；  
* 超过一个时间片的次数；  
* 各优先级队列长度；  
* 同一 hCPU 上 runnable vCPU 数。  
现有 state_ready_nsecs 只能表示累计 READY 时间，无法直接得到单次等待的最大值和 P99，因此需要单独增加单次等待统计。  
**采集周期**  

| 项目         | 周期           |
| ---------- | ------------ |
| READY 起点记录 | 每次进入 READY   |
| 等待时间计算     | 每次变为 RUNNING |
| 队列长度采样     | 100 ms       |
| P99、最大值汇总  | 1 s          |
| 持续超限判定     | 5 s          |
  
**阈值设置**  
READY 等待阈值应优先根据形式化模型计算，不应完全使用固定值。  
设：  
* 每核参与轮转的 vCPU 数量为 N；  
* 时间片为 Q；  
* 调度系统开销上界为 \Delta Sys。  
理论服务间隔上界为：  
L_{worst}=(N-1)(Q+\Delta Sys)+\Delta Sys  
建议阈值：  

| 等级  | 阈值                  |
| --- | ------------------- |
| 预警  | 等待时间超过 0.7L_{worst} |
| 严重  | 超过 0.9L_{worst}     |
| 硬超限 | 超过 L_{worst}        |
  
在无法立即得到 N 和理论上界时，以默认时间片 10 ms 为例，可暂用：  

| 指标                | 预警    | 严重    | 硬超限    |
| ----------------- | ----- | ----- | ------ |
| 最高优先级vCPU READY等待 | >2 ms | >5 ms | >10 ms |
  
对于 1:1 固定绑核、安全关键 vCPU，READY 等待正常应远小于 10 ms，因此可以设置更严格的阈值。  
   
⸻  
   
## 2.3 CPU 占用率  
**Hypervisor 适合监控的内容**  
Hypervisor 可以准确统计：  
* 每个 vCPU 的 RUNNING 时间；  
* 每个 Guest 所有 vCPU 的累计运行时间；  
* 每个 hCPU 的 idle 时间；  
* Hypervisor IRQ/异常时间；  
* Guest 域级 CPU 使用量。  
例如：  
U_{vcpu} = \frac{\Delta running\_nsecs} {\Delta window} \times100\%  
对于多 vCPU Guest，建议同时输出：  
1. 核等价值：  
U_{core-equivalent} = \frac{\sum \Delta running\_nsecs}{window}  
例如结果为 3.6，表示相当于占用 3.6 个物理核。  
2. 相对分配容量：  
U_{normalized} = \frac{\sum \Delta running\_nsecs} {window\times allocated\_vcpus} \times100\%  
**Hypervisor 不适合监控的内容**  
Hypervisor 无法准确知道：  
* Linux 中哪个进程占 CPU；  
* Android 哪个 UID、应用、Binder 线程占 CPU；  
* Guest 内部线程优先级和 cgroup 消耗；  
* 用户态、内核态、软中断的细粒度占比。  
这些应由 Guest Agent 采集：  
* Linux cgroup v2；  
* /proc/stat、/proc/<pid>/stat；  
* Android UID/cgroup；  
* PSI CPU pressure。  
**监控周期**  

| 指标                | 周期      |
| ----------------- | ------- |
| vCPU运行时间累计        | 调度切换时更新 |
| vCPU/Guest CPU占用率 | 1 s     |
| Guest内进程CPU占用     | 1 s     |
| 超限确认              | 5～10 s  |
| 趋势保存              | 1 min   |
  
**阈值建议**  

| 指标                 | 预警          | 严重         | 动作目标         |
| ------------------ | ----------- | ---------- | ------------ |
| Guest相对分配CPU占用     | >85%，持续10 s | >95%，持续5 s | 检查内部热点       |
| 单一非安全vCPU持续占满      | >95%，持续10 s | >99%，持续5 s | 域级限流候选       |
| Linux/Android进程CPU | >80%，持续10 s | >95%，持续5 s | cgroup/UID限流 |
| hCPU idle低于        | <10%，持续10 s | <2%，持续5 s  | 负载不均衡告警      |
  
细粒度限流应由 Guest Agent 使用 cgroup、cpuset 或 Android UID 控制完成。Hypervisor 更适合做 Guest/vCPU 粒度的粗粒度预算限制，不适合直接限制 Guest 内某个进程。  
安全域默认不建议自动限流；非安全 Linux/Android 域可启用自动限流。  
   
⸻  
   
## 2.4 异常指令周期与 Exit/Trap 诊断  
**监控主体**  
由 Hypervisor 监控。  
Hypervisor 能够直接获得：  
* vCPU；  
* Exit 原因 EC；  
* ESR、ISS；  
* PC；  
* FAR、HPFAR；  
* 单次 Exit 处理耗时；  
* 每秒 Exit 次数；  
* Exit 累计占用 CPU 时间。  
**监控方式**  
正常运行阶段只维护：  
```
exit_count[vcpu][ec]
exit_total_cost_ns[vcpu][ec]
exit_max_cost_ns[vcpu][ec]
exit_histogram[vcpu][ec]

```
诊断命令启动后，再开启短时间详细 trace：  
```
monitor diag exit --vcpu <id> --time 10

```
诊断输出应包含：  
* Top Exit 原因；  
* Exit/s；  
* 各 Exit 平均、P99、最大周期；  
* 最慢 Exit 对应的 PC、ESR、ISS；  
* 最近异常指令地址；  
* 相关调度延时和 IRQ 情况。  
**周期**  

| 项目        | 周期         |
| --------- | ---------- |
| Exit计数与耗时 | 每次Exit     |
| 短时速率      | 100 ms     |
| 每秒速率与P99  | 1 s        |
| 风暴判定      | 连续5 s      |
| 详细诊断trace | 按命令10～30 s |
  
**阈值建议**  

| 等级         | 条件                    |
| ---------- | --------------------- |
| 预警         | >20,000次/s，持续2 s      |
| 严重         | >50,000次/s，持续5 s      |
| 极端风暴       | >100,000次/s，持续1 s     |
| 单次Exit异常   | 单次处理>100 μs           |
| Exit时间占比预警 | Exit处理时间占vCPU运行时间>30% |
| Exit时间占比严重 | >60%，持续5 s            |
  
仅按次数判断可能误报。例如 WFI、合法 MMIO 或某些系统寄存器访问本身可能较频繁，因此应同时按：  
* Exit 原因；  
* Exit 速率；  
* 单次成本；  
* Exit 总时间占比；  
联合判定。  
   
⸻  
   
## 三、内存监控项细化  
## 3.1 Guest 物理内存使用量  
**Hypervisor 能监控的内容**  
在当前静态分区模型下，Hypervisor能够知道：  
* Guest 配置分配了多少物理内存；  
* Guest IPA 到 PA 的映射范围；  
* Stage-2 映射 Region 数量；  
* 是否存在缺失映射、重叠映射和越界映射；  
* 动态共享内存的分配量和引用关系。  
**Hypervisor不适合直接判断的内容**  
Hypervisor无法根据 Stage-2 映射判断：  
* Guest 哪些页正在使用；  
* 哪些页是 page cache；  
* 哪些页可回收；  
* Guest 真实内存压力；  
* Linux OOM 风险；  
* Android LMKD 压力；  
* CMA 水位。  
因为静态分配的 Guest 内存通常全部建立 Stage-2 映射，即使 Guest 内某页完全未使用，Hypervisor 看起来仍然是“已分配、已映射”。  
**建议职责**  

| 指标                  | 负责方         |
| ------------------- | ----------- |
| Guest配置内存总量         | Hypervisor  |
| Stage-2已映射量         | Hypervisor  |
| MemAvailable        | Guest Agent |
| 匿名页、缓存、swap         | Guest Agent |
| PSI memory pressure | Guest Agent |
| OOM/LMKD事件          | Guest Agent |
  
**周期与阈值**  

| 指标                 | 周期        | 预警          | 严重         | 紧急   |
| ------------------ | --------- | ----------- | ---------- | ---- |
| Guest MemAvailable | 1 s       | <20%，持续10 s | <10%，持续5 s | <5%  |
| Memory PSI full    | 1 s       | >5%，持续10 s  | >20%，持续5 s | 持续上升 |
| OOM/LMKD关键事件       | 事件触发      | 首次记录        | 连续发生       | 立即诊断 |
| Stage-2映射一致性       | 5 s及映射变更时 | 不适用         | 不适用        | 任意异常 |
  
   
⸻  
   
## 3.2 CMA 池水位  
**是否适合 Hypervisor 监控**  
不适合。  
CMA 是 Linux/Android Guest 内部的物理页分配机制，Hypervisor 无法从 Stage-2 页表中得知：  
* CMA 空闲页；  
* 已使用页；  
* 最大连续可用块；  
* 页迁移失败；  
* CMA 碎片化。  
**建议由 Guest Agent 监控**  
监控指标：  
* CmaTotal；  
* CmaFree；  
* CMA 使用率；  
* 最大连续可分配块；  
* CMA 分配失败次数；  
* 最近最大申请尺寸。  
**周期与阈值**  

| 指标      | 周期   | 预警          | 严重         | 紧急   |
| ------- | ---- | ----------- | ---------- | ---- |
| CMA剩余比例 | 5 s  | <20%，持续10 s | <10%，持续5 s | <5%  |
| 最大连续块   | 5 s  | <最大业务请求×2   | <最大业务请求    | 分配失败 |
| CMA分配失败 | 事件触发 | 首次记录        | 3次/10 s    | 持续失败 |
  
CMA 不能只看总剩余量。即使 CmaFree 较高，也可能因为碎片化无法提供大块连续内存。  
   
⸻  
   
## 3.3 Guest 和共享内存边界异常  
**监控主体**  
由 Hypervisor 监控，而且属于最适合由 Hypervisor 监控的内存安全指标。  
**监控对象**  
* Guest IPA 越出配置范围；  
* Guest 访问其他 Guest 内存；  
* 共享内存 owner 不匹配；  
* 映射范围重叠；  
* 共享内存越界；  
* 读写权限异常；  
* 非法重复映射；  
* 解映射不存在的 Region；  
* Stage-2 instruction/data abort。  
**采集周期**  
这类异常不应轮询，应按事件实时监控：  

| 项目              | 周期        |
| --------------- | --------- |
| map/unmap边界检查   | 每次操作      |
| Stage-2 fault检查 | 每次fault   |
| 共享内存权限检查        | 每次映射和访问异常 |
| 异常计数汇总          | 1 s       |
| 重复异常判定          | 1 s/5 s   |
  
**阈值**  
安全边界异常原则上为零容忍：  

| 条件          | 阈值目标                  |
| ----------- | --------------------- |
| IPA越界       | 1次即严重告警               |
| 跨Guest访问    | 1次即严重告警               |
| 共享内存owner异常 | 1次即告警                 |
| Stage-2权限异常 | 1次即保存上下文              |
| 同一vCPU重复异常  | 3次/1 s进入隔离候选          |
| 连续异常        | 10次/5 s考虑重置vCPU或Guest |
  
不能等到达到百分比阈值后才处理内存安全边界问题。  
   
⸻  
   
## 3.4 IOMMU 页表请求  
**监控主体**  
由 Hypervisor 监控，前提是 IOMMU/SMMU 的 map、unmap 和 fault 路径由 Hypervisor 管理。  
**需要明确“异常请求”的定义**  
总请求数：  
N_{total} = N_{map}+N_{unmap}+N_{remap}  
异常请求数：  
N_{abnormal} = N_{invalid\_iova} +N_{out\_of\_range} +N_{duplicate} +N_{unmap\_missing} +N_{permission} +N_{owner\_mismatch}  
异常比例：  
R_{iommu} = \frac{N_{abnormal}}{N_{total}}\times100\%  
**周期**  

| 项目      | 周期     |
| ------- | ------ |
| 请求和错误计数 | 每次请求   |
| 短时速率    | 100 ms |
| 异常比例计算  | 1 s    |
| 持续超限判定  | 5 s    |
| 趋势保存    | 1 min  |
  
**阈值建议**  
比例阈值必须设置最小样本数。否则 1 次请求失败就会得到 100%。  

| 条件             | 阈值                  |
| -------------- | ------------------- |
| 比例判定最小样本       | 5 s内总请求≥100         |
| 预警             | 异常比例>10%，连续3个窗口     |
| 严重             | >20%，连续5 s          |
| 极端异常           | >50%，持续1 s          |
| IOVA越界/权限fault | 任意1次立即告警            |
| 同一设备持续严重       | 10 s内仍>20%，进入设备重置候选 |
  
IOMMU 限流应限制 map/unmap 控制请求，不应直接限制正常 DMA 数据通路。  
   
⸻  
   
## 3.5 建议补充的 Hypervisor 自身内存指标  
虽然需求重点是 Guest，HOS 自身也建议监控：  

| 指标          | 周期   | 预警       | 严重      | 紧急   |
| ----------- | ---- | -------- | ------- | ---- |
| HOS heap剩余率 | 1 s  | <20%     | <10%    | <5%  |
| Pagepool剩余率 | 1 s  | <15%     | <8%     | <3%  |
| 分配失败次数      | 每次失败 | 首次记录     | 3次/10 s | 持续失败 |
| 最大连续块       | 5 s  | 低于业务需求×2 | 低于业务需求  | 分配失败 |
  
   
⸻  
   
## 四、中断监控项细化  
## 4.1 物理 IRQ 到虚拟注入端到端延时  
**Hypervisor可以监控的范围**  
Hypervisor 可以测量：  
```
EL2软件可见IRQ入口
    →
host IRQ分发
    →
虚拟IRQ映射
    →
vmm_vcpu_irq_assert/VGIC注入完成

```
定义：  
T_{inject} = T_{virtual\_inject\_done} - T_{physical\_irq\_entry}  
**Hypervisor无法单独测量的部分**  
仅靠 Hypervisor 软件无法准确测量：  
* 外设真正拉高中断线的时刻；  
* GIC 内部传播延时；  
* 虚拟 IRQ 注入后 Guest handler 真正开始执行的时刻；  
* Guest 最终完成中断处理的时刻。  
若需求要求完整到 Guest handler，需要 Guest Agent 增加时间戳或使用硬件追踪。  
**监控周期**  

| 项目                | 周期      |
| ----------------- | ------- |
| 延时时间戳             | 每次目标IRQ |
| 直方图更新             | 每次IRQ   |
| P50/P95/P99/P99.9 | 1 s     |
| 持续超限判定            | 5 s     |
| 长期趋势              | 1 min   |
  
对于高频普通 IRQ，可以按比例采样；安全关键 IRQ 建议全量采集。  
**建议统计值**  
* 平均值；  
* 最大值；  
* P50；  
* P99；  
* P99.9；  
* 抖动：  
Jitter=P99-P50  
**初始阈值建议**  

| IRQ类型       | P99预警   | P99严重   | 单次硬阈值    |
| ----------- | ------- | ------- | -------- |
| 安全关键IRQ     | >50 μs  | >100 μs | >500 μs  |
| 普通设备IRQ     | >100 μs | >500 μs | >2 ms    |
| 调度Timer IRQ | 基线P99×2 | 基线P99×4 | 根据调度预算设置 |
  
如果安全业务已经规定最大响应时限，应优先采用业务时限，而不是通用阈值。  
   
⸻  
   
## 4.2 Exit 陷入风暴  
**监控主体**  
由 Hypervisor 监控。  
**监控维度**  
必须按以下维度分别计数：  
* Guest；  
* vCPU；  
* Exit EC；  
* PC；  
* hCPU；  
* 单次处理耗时。  
不能只监控全系统 Exit 总数，否则单个异常 vCPU 会被其他正常 vCPU 稀释。  
**周期**  

| 项目          | 周期     |
| ----------- | ------ |
| Exit计数      | 每次Exit |
| 100 ms桶计数   | 100 ms |
| Exit/s      | 1 s    |
| 持续风暴判定      | 5 s    |
| Top PC/EC诊断 | 触发时    |
  
**阈值建议**  
按需求中的 5 万次/s 设置严重门限：  

| 等级             | 阈值                |
| -------------- | ----------------- |
| 预警             | >20,000次/s，持续2 s  |
| 严重             | >50,000次/s，连续5 s  |
| 极端             | >100,000次/s，持续1 s |
| Exit CPU时间占比预警 | >30%              |
| Exit CPU时间占比严重 | >60%，持续5 s        |
  
满足严重条件时，应至少生成诊断数据：  
* vCPU寄存器；  
* ESR、ISS、PC；  
* Top Exit原因；  
* Exit速率；  
* 最近调度延时；  
* 最近IRQ情况。  
是否自动重置应区分安全域与非安全域。安全域不宜仅因速率超限直接重置，应先按安全策略执行受控隔离或降级。  
   
⸻  
   
## 4.3 异常硬件中断、注入失败和丢失中断  
**Hypervisor适合监控的内容**  
Hypervisor 可以直接检测：  
* 物理 IRQ 接收次数；  
* 虚拟 IRQ 注入尝试次数；  
* 注入成功次数；  
* 注入失败次数；  
* VGIC LR 不足；  
* 目标 vCPU 不可运行；  
* IRQ pending 时间；  
* 同一 IRQ 占总 IRQ 比例；  
* IRQ 是否被 Hypervisor 屏蔽。  
**Hypervisor不能单独证明的内容**  
不能仅通过：  
```
物理IRQ次数 != 虚拟注入次数

```
就认定中断丢失。  
原因包括：  
* level IRQ 合并；  
* IRQ coalescing；  
* Guest mask；  
* vCPU暂停；  
* 同一中断已经 pending；  
* 设备合法合并多个事件。  
真正判断中断丢失，通常需要：  
* 设备事件序号；  
* 物理 IRQ 计数；  
* 虚拟注入序号；  
* Guest 接收或 EOI 序号；  
联合验证。  
**周期**  

| 项目          | 周期     |
| ----------- | ------ |
| IRQ计数与注入结果  | 每次IRQ  |
| IRQ占比       | 1 s    |
| 注入失败率       | 1 s    |
| 持续超限判定      | 5 s    |
| pending超时检查 | 100 ms |
| 长期趋势        | 1 min  |
  
**IRQ 占比阈值**  
定义：  
R_{irq} = \frac{N_{irq_i}}{N_{irq\_total}} \times100\%  
需要设置最小总样本：  

| 条件       | 阈值               |
| -------- | ---------------- |
| 占比判定最小样本 | 总IRQ≥1000次/s     |
| 预警       | 单IRQ占比>60%，持续3 s |
| 严重       | >80%，持续5 s       |
| 极端       | >90%，持续2 s       |
  
部分正常 IRQ，例如调度 Timer，可能天然占比较高，必须支持白名单和按设备类型配置阈值。  
**注入失败与 pending 阈值**  

| 指标        | 安全关键IRQ  | 普通IRQ                  |
| --------- | -------- | ---------------------- |
| 注入失败      | 任意1次严重告警 | >10次/s或失败率>0.01%，持续5 s |
| pending超时 | >500 μs  | >5 ms                  |
| 无来源虚拟注入   | 任意1次     | 任意1次记录                 |
| 中断疑似丢失    | 任意1次告警   | 连续3次或失败率超限             |
  
对于安全关键 IRQ，不建议自动屏蔽；优先选择设备重置、路由切换或系统降级。对于非安全设备 IRQ，可以按策略限流、屏蔽或重置设备。  
   
⸻  
   
## 五、阈值设置目标  
阈值不应只写成一个绝对数字，应同时满足以下目标。  
## 5.1 早于业务失效触发  
若业务硬约束为 L_{hard}，建议：  
L_{warning}=0.7L_{hard}  
L_{critical}=0.9L_{hard}  
例如安全任务要求响应时间小于 50 ms：  

| 等级  | 阈值    |
| --- | ----- |
| 预警  | 35 ms |
| 严重  | 45 ms |
| 硬超限 | 50 ms |
  
   
⸻  
   
## 5.2 避免瞬态误报  
阈值判定应同时包含：  
* 数值阈值；  
* 最小样本数；  
* 持续时间；  
* 恢复回滞。  
例如：  
```
连续3个窗口超过预警值 → WARNING
连续5秒超过严重值 → CRITICAL
连续10秒低于恢复值 → 恢复NORMAL

```
恢复阈值建议取预警值的 70%～80%。  
   
⸻  
   
## 5.3 区分安全域与非安全域  

| 监控对象   | 安全域        | 非安全域      |
| ------ | ---------- | --------- |
| CPU超限  | 优先保障、告警    | 可限流       |
| 内存不足   | 保留资源、降级    | 可回收或限额    |
| Exit风暴 | 诊断、隔离      | 可暂停或重置    |
| IRQ风暴  | 关键IRQ不直接屏蔽 | 可限流、屏蔽、重置 |
| 边界异常   | 首次即严重处理    | 首次即隔离     |
  
   
⸻  
   
## 六、建议的统一周期与初始阈值  

| 监控项 | 负责方 | 采集方式 | 判定周期 | 初始严重阈值 |
| ------------------ | ----------- | --------------- | ---------- | ------------ |
| A→C调度延时 | Hypervisor | 每次调度事件 | 1 s/5 s | P99>10 μs |
| READY等待 | Hypervisor | 每次READY→RUNNING | 1 s/5 s | >0.9理论上界 |
| vCPU CPU占用 | Hypervisor | 调度累计 | 1 s/5 s | >95% |
| 进程CPU占用 | Guest Agent | cgroup/proc | 1 s/10 s | >95% |
| Exit风暴 | Hypervisor | 每次Exit | 100 ms/5 s | >50,000次/s |
| Guest MemAvailable | Guest Agent | /proc/meminfo | 1 s/10 s | <10% |
| CMA剩余 | Guest Agent | CMA统计 | 5 s/10 s | <10% |
| 内存边界异常 | Hypervisor | 事件触发 | 立即 | 任意1次 |
| IOMMU异常比例 | Hypervisor | 每次请求 | 1 s/5 s | >20% |
| IRQ注入P99 | Hypervisor | 每次IRQ | 1 s/5 s | 安全IRQ>100 μs |
| 单IRQ占比 | Hypervisor | 每次IRQ | 1 s/5 s | >80% |
| 安全IRQ注入失败 | Hypervisor | 每次注入 | 立即 | 任意1次 |
  
这套划分的核心是：  
* **调度、Exit、Stage-2、IOMMU、IRQ 注入由 Hypervisor监控；**  
* **Guest 内部 CPU、真实内存压力和 CMA 由 Linux/Android Agent 监控；**  
* **跨越 Hypervisor 与 Guest 边界的指标，例如 Guest 中断处理完成和真正丢失中断，需要双方联合验证。**  
